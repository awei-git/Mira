# API Keys and Secrets
# Copy to secrets.yml and fill in your values. secrets.yml is gitignored.

api_keys:
  openai:
    api_key: "sk-..."
    organization: "org-..."

  deepseek: "sk-..."

  gemini: "AIza..."

  # Optional — needed for publish agent
  # substack:
  #   subdomain: "[your-blog]"
  #   cookie: "s%3A..."
