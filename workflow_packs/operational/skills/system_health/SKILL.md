# System Health

Detect operational failures and record durable lessons.

Required behavior:

- Do not create a scar for a transient green run.
- Create a scar when a failure pattern needs future behavior change.
- Keep health output local and traceable.
