# Mira V3.1 Implementation Review

Date: 2026-05-21

## Scope

Reviewed the V3.1 implementation surface against `docs/v3.1-architecture.html`, with emphasis on executable modules under `lib/mira`, workflow packs, dashboard queues, and the V3 test suite.

## Current State

| Area | Status | Notes |
|---|---:|---|
| Experience ledger / delta / commit | Implemented | `record_experience`, `PipelineExecutor`, `ExperienceLedger`, `MemoryCommitLog`, and gateway-backed commits are wired. |
| Memory security gateway | Implemented, still heuristic | Blocks untrusted writes, duplicate memory, obvious injection, policy-bypass intent, secret-like material, unsupported causal claims, evidence-free hypotheses, and high/critical risk memory changes. Findings now expose structured type/severity for review queues; deterministic semantic contradiction checks cover simple positive/negative preference and policy conflicts, public/private routing reversals, and conflicting numeric operating constraints. A reusable poisoning red-team harness now checks these boundaries. |
| Workflow authoring layer | Implemented MVP | All 20 configured non-communication catalog workflows now have executable MVP packs; `communication` remains a native V3 pipeline. Conservative route decisions now expose missing connectors, fallback degradation, ambiguity, confirmation need, and confidence before execution. The intelligence briefing pack now writes §17.2 source-fetch records, dedupes duplicate source URLs into a trust-classified source bundle, and feeds the deduped bundle into the briefing writer. |
| Workflow security audit | Strengthened | Compilation now audits the command plus pack-level files and sibling skill metadata/Markdown before enablement, blocks suspicious payloads, undeclared live tool use, privacy downgrades, public effects without matching risk approval, and stale risk/effect step references; it computes file hashes, signs audit artifacts, supports tamper verification, key rotation, and explicit trust-bundle import/export for cross-directory verification. Generated/imported skill candidates now have an in-memory runtime audit gate before any save or enablement path. |
| Capability preflight | Implemented | Required connector gating blocks workflows before side effects. Capability checks now carry the V3.1 schema fields: connector, status, scopes, risk tier, last-checked timestamp, fallback, and required/optional classification. Successful degraded runs record durable `preflight_degraded:*` eval refs; blocked runs record `preflight_missing:*` refs. |
| Action risk approval | Implemented MVP | Risk grants and approval queue exist; public publish, external provider calls, and financial/health external writes are blocked without approval, approval tokens are bound to preview hashes, approval-gated side effects carry the grant id, overdue approval requests cannot be granted after their expiry window, new non-critical public/code/memory approval requests are auto-paused when the queue is over budget, and preview-bound low-risk public approvals are surfaced as a non-mutating batch digest for faster review. |
| Causal trace | Implemented MVP | Causal links now reference persisted causal evidence records; native communication and all executable workflow-pack paths produce L4 ablation evidence when prior same-pipeline context changes deterministic behavior. Latest A2A trust and self-evolution causal samples are hard-gated for L4 evidence, §22.8 `CausalTrace` records can be built for important behavior coverage audits, and the status matrix now verifies the L1/L3/L4 evidence contract plus live trace completeness. |
| Snapshot builder | Implemented MVP | Snapshot manifests include run/profile identifiers, scoring/exclusion data, token counts, and stable hashes. Snapshot items now expose memory id, inclusion rationale, trust/privacy tier, and relevance/recency/importance/causal-success/trust/privacy/diversity/token-budget score breakdowns. |
| Eval / north-star scorecards | Implemented MVP | Operational and strategic scorecards are exposed in dashboard snapshot; hard gates now include causal validity, required L4 evidence, unresolved effects, unreplayable public/memory/code effects, and executed high-risk effects missing approval-token/preview binding. Dashboard/status JSON now exposes operational score components and metric details, while the strategic scorecard separately exposes A2A experiments, artifacts, tool prototypes, public writeups, actual external/public feedback events, product thesis updates, commercial options, shared watch gates, and briefing feedback coverage without counting generic strategic-plan refs as received feedback or orphan publication-feedback refs without a matching shipped writeup slug. The strategic score now follows the normalized §23.4 formula, so missing external feedback cannot be hidden by over-weighted local progress. Operator-supplied publication evidence can now be recorded through `v3_record_public_evidence.py`, which validates concrete public URLs, optional draft preview hashes, and the deterministic public-writeup safety audit before appending `public_writeup:*` refs; later feedback can be recorded through `v3_record_public_feedback.py` as `external_feedback:*` only after the matching writeup slug is already recorded and without duplicating the writeup count. The no-network feedback solicitation path now creates local request/checklist/metadata packets through `v3_prepare_public_feedback_packet.py` before any feedback is counted. Operator-supplied briefing feedback can now be recorded through `v3_record_briefing_feedback.py`, which validates the button alias and requires the item id to exist in the current weekly blind-sample queue before appending `briefing_feedback:*` refs. Eval 4 now treats feedback-dependent action, dismiss, and novel-but-relevant metrics as unmeasured until operator feedback exists, and uses total scored briefing items as the denominator once feedback is present. A weekly markdown report CLI persists the §23 north-star scorecard as an artifact, exposes non-hard-gate metric failures, derives V3.1 `FailureEvent` / `FailureSignatureEval` recurrence metrics for Eval 1, derives V3.1 `EvalRecord`, `OutcomeRecord`, `DecisionRecord`, and `BehavioralEffect` rows from durable run evidence, carries V3.1 `ApprovalEvent` rows into the eval bundle, groups those rows into per-run V3.1 evidence bundles, derives V3.1 `ExperimentRecord` coverage for Eval 5, derives V3.1 `IncidentEvent` records for Eval 6, tracks briefing feedback buttons plus weekly blind-sample review/dashboard queue metrics for Eval 4, derives V3.1 `MemoryAuditRecord` metrics for Eval 7, and now renders dashboard-backed next actions with the public safety/packet/evidence/feedback commands, briefing feedback recorder command, and provider env-template/readiness/runbook/canary commands. The §24 named eval checklist is now executable through dedicated CLIs: `repeated_error_eval.py`, `causal_memory_eval.py`, `trace_completeness_eval.py`, `voice_eval.py`, `briefing_interest_eval.py`, `memory_audit.py`, and `approval_safety_eval.py`. |
| Baselines / status matrix | Implemented | `v3_baseline_capture.py` writes the six Phase -1 baseline artifacts from the same V3.1 eval engine used by the weekly report, excluding known synthetic task fixtures and including causal evidence. Dashboard/status JSON now exposes a visible implementation status matrix with section, V3.1 plan reference, status, evidence, tests, missing-evidence/test lists, verifier checks, owner, and next gate; the ledger row validates durable ExperienceRecord JSONL shape and unique run ids, Memory Gateway runs the poisoning red-team harness and validates live commit-log safety, Capability Preflight validates registry metadata plus degrade/block behavior, Provider Effect Adapters validate approval-bound execution, idempotent reuse, unknown/failed transitions, deployment/health/rollback adapter orchestration, env-backed config shape, and live succeeded-effect approval metadata, Approval Queue validates risk policy, preview-bound grants, expiry, capacity throttling, event visibility, and live unexpired grant safety, Causal Trace validates the synthetic evidence-level contract and live important-behavior trace completeness, Snapshot Builder validates manifest identity, scoring breakdowns, privacy exclusion, token accounting, and recent-experience inclusion, the Effect Log row validates append-only effect-log rows and latest succeeded execution timestamps, Web Review Queues validate all §16 queues, the §16.6 approval digest, the strategic public-writeup review queue, the public-feedback follow-up queue, the customer-discovery feedback queue, the effect-reconciliation inspector path in the remaining-gates handoff, the provider-provisioning blocker queue, and their required one-screen context, Legacy Runtime Bridge validates task/background routing, durable records, gate behavior, snapshot export, and post-hook wiring, Baselines carry a latest-complete-artifact-set verifier, North Star Evals carry a live non-mutating eval-gate check plus explicit §14.4 threshold-change governance and public-feedback integrity checks, Workflow Packs verify that malicious generated/imported skill candidates are blocked before enablement, that every non-native runtime pipeline has a registered compiling workflow pack with the expected name and memory class, and that the intelligence briefing source-fetch/dedup/trust-bundle contract executes, and provider production readiness is explicitly marked `blocked_external` with a passing non-mutating canary-surface verifier plus a machine-readable readiness check until credentialed env vars pass. |
| Durable runtime / effect log | Implemented MVP | Idempotency keys, open/unknown status, retry blocking, reconciliation queue, before/after side-effect checkpoints, preview hashes, provider refs, reconciliation refs, replay bundle refs for effect actions, local provider-state resolvers for publish/RSS/social/market/health evidence, pluggable connector-backed provider resolvers, a read-only HTTP JSON provider resolver, approved provider adapter execution, local provider-state execution, local RSS feed publication, local provider dress rehearsal, approval-gated git production promotion/rollback, opt-in deployment-service execution with health verification and rollback, and runtime JSON config wiring are present. |
| Web review queues | Implemented MVP | Approval, approval digest, memory commit, experiment, incident, effect reconciliation, briefing-feedback, public-writeup-review, public-feedback-followup, and provider-provisioning queues are exposed. Approval queue exposes preview hashes, expiry, age, evidence, rollback note, and decision state; approval digest batches multiple pending preview-bound low-risk public approvals into a review-only item with request ids, hashes, expiry, estimated review minutes, and rollback context; memory commit queue exposes source trust, memory class, risk level, privacy tier, evidence refs, contradictions, and available operator decisions; dashboard capacity reports pending count, daily review budget, queue-age pressure, and auto-pause flag; experiment queue includes hypothesis evidence counts, latest evidence, baseline window, test window, `min_n`, current metric, and rollback plan with legacy fallbacks for older hypotheses; effect reconciliation exposes preview hashes, replay bundle refs, provider refs, reconciliation refs, and approval grant ids; briefing feedback exposes weekly blind-sample item ids, topics, matched interests, scores, button-ref templates, and a `v3_record_briefing_feedback.py` command template; public-writeup review exposes draft artifact, title, preview hash, publication safety status/findings, publish ref template, feedback ref template, a `v3_public_writeup_safety.py` command, a `v3_prepare_public_writeup_packet.py` command, deterministic publication packet artifact, packet-based `v3_record_public_evidence.py --packet ...` command template, hash-bound explicit fallback evidence command, and an explicit `needs_publication_review` decision without counting the draft as shipped; public-feedback follow-up exposes recorded publication URL, latest publication stats, current zero-feedback state, feedback ref template, a `v3_prepare_public_feedback_packet.py` command, and the feedback-only `v3_record_public_feedback.py` command; provider provisioning exposes readiness finding counts, missing env vars, config paths, env-template and runbook artifact paths, full and scoped env-template/readiness/canary commands, and a smallest-missing-env first canary while keeping the production gate `blocked_external`. Every queue item now carries one-screen review context for why now, what changes, supporting evidence, risk, and rollback. |
| Legacy runtime bridge | Implemented MVP | Legacy background jobs and task completions can write V3 experiences and prepare snapshots. Human gates and preflight guardrails no longer create failure-memory writes. |

## Work Completed In This Pass

- Added bundle-level workflow audit so a workflow command cannot enable a malicious `skill.yaml` or `SKILL.md` in the same pack.
- Added regression coverage proving compilation fails when a referenced skill Markdown contains a suspicious remote shell payload.
- Ran the local `a2a_trust_experiment` workflow once, producing an artifact and active hypothesis for the strategic north-star loop.
- Added persisted workflow audit artifacts with deterministic audit hashes for runtime workflow compilation.
- Ran the local `system_health` workflow once, producing `data/v3/workflow_audits/system_health-0ccff264c416.json`.
- Corrected the operational `causal_link_validity` hard gate so it rates asserted causal links rather than routine records with no causal claim.
- Added `data/v3/causal_evidence.jsonl` as the persisted causal evidence log and wired dashboard scoring to validate causal links against it.
- Ran the communication pipeline twice; the second run used prior memory, changed reply behavior, and now writes an L4 causal evidence record with a counterfactual reply baseline.
- Extended L3 causal evidence emission to repeated `article_creation` and `a2a_trust_experiment` workflow runs.
- Added dashboard/status visibility for causal evidence counts by level.
- Added executable MVP workflow packs for `podcast_production`, `self_evolution`, and `memory_maintenance`.
- Added local-only deterministic actions for podcast pre-publish planning, self-evolution experiment recording, and memory maintenance review-before-compaction.
- Registered the three new packs in the runtime path so they use the same compile audit, preflight, ledger, memory commit, causal evidence, and review queue path as the earlier representative workflows.
- Ran live smoke workflows for `podcast_production`, `self_evolution`, and `memory_maintenance`, producing artifacts and persisted L3 causal evidence.
- Added executable MVP workflow packs for `social_reactive`, `social_proactive`, and `weekly_growth_report`.
- Added local-only deterministic actions for reactive reply drafting, proactive note planning, and weekly growth reporting, with live platform writes kept out of the MVP path.
- Ran live smoke workflows for the three social/growth packs, producing artifacts, audit records, and persisted L3 causal evidence.
- Added executable MVP workflow packs for the remaining configured non-communication paths: `book_reading_notes`, `research_deep_dive`, `daily_thought_discussion`, `daily_journal`, `weekly_reflection`, `market_monitor`, `incident_response`, `health_wellness`, `skill_learning`, and `deterministic_reference`.
- Added a shared local-report workflow action for safe artifact-first execution across non-side-effecting MVP paths.
- Ran live smoke workflows for the remaining packs; runtime audit artifacts now exist for all 20 registered workflow packs.
- Added ablation-confirmed L4 causal evidence generation for high-impact `a2a_trust_experiment` and `self_evolution` workflow samples.
- Added an operational hard gate requiring the latest A2A trust and self-evolution causal samples to link to L4 evidence with an ablation reference.
- Exposed `causal_link_validity` and `l4_required_causal_evidence` in the dashboard operational scorecard.
- Extended workflow-pack security audit to block undeclared live connector/tool use and private/local-only memory being routed into public workflow context.
- Added structured gateway finding type and severity fields, and surfaced them in the dashboard memory-commit review queue.
- Extended executable `self_evolution` pack through the V3.1 canary/observe/confirm/reject/rollback phase.
- Added a local self-evolution canary/rollback artifact and gateway-reviewed hypothesis evidence update.
- Fixed executor trace merging so multi-step workflows aggregate artifacts, eval refs, and side-effect refs instead of overwriting earlier step outputs.
- Added HMAC-SHA256 signing for workflow audit artifacts using a local untracked signing key in the audit directory.
- Added workflow audit artifact verification and regression coverage proving tampered artifacts fail verification.
- Added L3 causal evidence emission for repeated `system_health` and `intelligence_briefing` runs when prior same-pipeline memory changes the run into a comparison point.
- Added explicit effect-status overrides so side-effect adapters can leave effects `planned`, `unknown`, or `failed` instead of being marked successful just because the step returned.
- Changed the article MVP publish path to leave an approved Substack publish as a staged reconciliation item until a real external adapter confirms completion.
- Narrowed the incident dead-letter queue so approval gates are not reported as incidents.
- Added executor-level retry protection for side effects: completed effects are reused without rerunning the tool, stale `executing` effects become `unknown`, and `unknown` effects block with `reconciliation_required` until reconciled.
- Added V3.1 EffectRecord reconciliation metadata: step/action identity, preview hash, approval token slot, external provider ref, execution timestamp, and reconciliation ref.
- Wired approval grant ids into approval-gated effect records so public publish side effects carry an auditable approval token.
- Surfaced approval grant ids in the dashboard effect reconciliation queue alongside preview hashes and provider reconciliation refs.
- Bound approval grants to the approved preview hash so payload changes after approval require a fresh approval request before any side effect can run.
- Added typed `ReconciliationResult` support so resolver code can persist provider evidence instead of only returning a boolean.
- Added local provider-state effect resolvers for Substack publish manifest entries and RSS feed XML, with runtime wiring through `reconcile_provider_effects`.
- Added YAML-level workflow audit checks that block public/external effect declarations without matching risk approval and catch stale risk/effect entries that reference nonexistent steps.
- Added a north-star hard gate that fails executed high-risk effects unless the current effect state carries both an approval grant id and the approved preview hash; evals now score latest effect state per idempotency key.
- Added a conservative V3.1 workflow router with structured `RouteDecision` output for task, background-job, and explicit workflow routing; background context exports the route decision JSON alongside the memory snapshot.
- Added a policy-intent gateway check so memory proposals that try to bypass, disable, lower, or relax approval/safety/gateway policy require human review with a structured `policy_bypass` finding type.
- Added workflow audit signing-key rotation, a local verification keyring, and 0600 trust-bundle export/import so old artifacts survive rotation and another audit directory can verify imported workflow audit signatures.
- Added local N-run self-evolution canary observation: repeated `self_evolution` runs now mark the canary as observing, confirmed, or rejected based on prior same-pipeline runtime records, and expose the status through artifact text, eval refs, and hypothesis evidence.
- Added a connector-backed effect reconciliation hook: callers can pass provider resolvers into `reconcile_provider_effects`, and unknown effects reconcile only when a connector returns structured success or failure evidence.
- Broadened L4 causal evidence emission for public-facing staged workflows: repeated `article_creation`, `podcast_production`, `social_reactive`, `social_proactive`, and `weekly_growth_report` runs now generate ablation-confirmed evidence for their memory-influenced artifact/staging decisions.
- Added a broader deterministic semantic contradiction check in the memory gateway so positive/negative preference and policy memories about the same object require human review instead of being silently committed.
- Added explicit workflow audit trust-bundle provenance and import policy: exported bundles carry operator/scope metadata, imports can enforce a trusted-operator allowlist, and imported HMAC keys remain verify-only unless symmetric signer activation is explicitly allowed.
- Broadened repeated-run L4 causal evidence for the remaining named local MVP gaps: `research_deep_dive`, `market_monitor`, `incident_response`, `health_wellness`, and `memory_maintenance` now emit ablation-confirmed evidence when prior same-pipeline context changes the local report.
- Closed the remaining workflow-pack L4 coverage gap: repeated runs of `system_health`, `intelligence_briefing`, `book_reading_notes`, `daily_thought_discussion`, `daily_journal`, `weekly_reflection`, `skill_learning`, and `deterministic_reference` now also emit ablation-confirmed evidence instead of L3-only evidence.
- Promoted native `communication` memory-effect evidence to L4: when prior relationship memory changes the reply from the baseline prefix to `Short answer:`, the pipeline now records the counterfactual reply without that memory and stores an ablation ref.
- Deepened deterministic semantic contradiction detection in the memory gateway: public/private routing reversals such as local-only drafts becoming public-before-approval, and conflicting numeric operating constraints such as quota changes, now require human review.
- Added a concrete read-only HTTP JSON effect resolver for provider reconciliation: unknown effects can now query a provider API endpoint through an injectable `httpx` client, normalize published/error/not-found payloads into reconciliation refs, and leave server/transport failures unresolved for retry.
- Wired provider resolver configuration into the runtime: `data/v3/provider_resolvers.json` can now declare HTTP JSON provider endpoints, bearer-token environment lookups, response payload paths, and injected clients for testable reconciliation.
- Extended provider reconciliation from `unknown` effects to all open effect states, so staged `planned` article, RSS, and social effects can be reconciled when provider state positively confirms completion.
- Added provider resolver config validation so unsafe production reconciliation entries are skipped: HTTP endpoints must use HTTPS, include an effect identity field, and use environment-based bearer-token provisioning unless inline secrets are explicitly allowed.
- Added a memory-driven podcast TTS routing step: prior MiniMax/TTS failure scars now route `podcast_production` to `fallback_tts`, write a `tts_route.json` artifact, and persist L4 ablation evidence showing the route changed because of memory.
- Added governed podcast RSS publish staging: `podcast_production` now writes an RSS-only preview artifact, skips public publish when the RSS connector is absent, and when the connector is present requires approval before leaving a planned `publish_rss` effect for reconciliation.
- Added governed social publish staging: `social_reactive` and `social_proactive` now write social publish preview artifacts, degrade to local staging when platform connectors are absent, and require approval before leaving planned `post_social` effects for reconciliation.
- Added opt-in self-evolution branch canary execution: `self_evolution` can now create a real local git canary branch, write an auditable branch-canary artifact, and roll back to the original ref when requested.
- Added governed market and health external-write staging: `market_monitor` now writes a market alert preview and approval-gates planned `send_market_alert` effects; `health_wellness` now writes a health write preview and approval-gates planned `write_health` effects. Both degrade to local staging when the relevant connector is absent.
- Added explicit `financial_external` and `health_external` risk classes to keep portfolio-facing and health-provider writes separate from public publishing while preserving approval-token and preview-hash binding.
- Extended the north-star high-risk effect hard gate so executed market/portfolio/health effects without approval-token and preview-hash binding fail the operational scorecard.
- Added generic local provider-state manifest reconciliation for staged `post_social`, `send_market_alert`, and `write_health` effects. Runtime now scans `data/v3/provider_state/*.json` and reconciles open effects when structured provider evidence proves posted/sent/synced or rejected/error states.
- Added production provider resolver profiles for Substack, RSS, social, market, and health reconciliation. Profiles use env-backed HTTPS endpoint templates and bearer-token env vars only; `config/v3/provider_resolvers.production.example.json` provides the no-secret template for `data/v3/provider_resolvers.json`.
- Added governed memory compaction staging: `memory_maintenance` now writes a compaction preview artifact by default, stays review-only unless `compaction_enabled` is set, and requires destructive approval before leaving a planned `compact_memory` effect. The workflow still performs no kernel mutation itself.
- Extended the north-star high-risk effect hard gate so executed `compact_memory` effects without approval-token and preview-hash binding fail the operational scorecard.
- Added a local memory compaction adapter: approved planned `compact_memory` effects can now archive selected scars, skill traces, failure signatures, or relationship notes into a durable `archived_memories` kernel section, remove them from active memory surfaces, and mark the effect succeeded. The adapter refuses to run without approval-token and preview-hash metadata.
- Added deterministic compaction candidate promotion: `memory_maintenance` now recommends low-decay skill traces, inert failure signatures, and explicitly stale/duplicate/superseded/obsolete/archive-marked items in the preview artifact. When `compaction_enabled` is set and no manual candidates are provided, those recommendations become the staged compaction candidates.
- Added governed self-evolution production promotion controls: `self_evolution` now writes a production promotion preview artifact, approval-gates planned `promote_production` effects under `code_config` risk, and exposes a runtime adapter that refuses to run without approval metadata, a clean local git repo, named production/canary branches, and a fast-forward-only promotion path. The adapter records rollback refs and can execute an immediate rollback when requested.
- Added explicit remote git production push/rollback support for self-evolution promotion: the preview can opt into a named remote and branch, the adapter records remote refs before/after promotion, and rollback uses `--force-with-lease` pinned to the promoted SHA so newer remote work is not overwritten.
- Added an opt-in deployment-service adapter path for self-evolution production promotion: approved promotion previews can request deployment execution, the runtime loads a `deployment` HTTPS JSON adapter from the same env-backed no-secret provider adapter config, sends the approved preview plus promoted ref, records deployment evidence in the promotion result artifact, and leaves the effect `unknown` instead of claiming success when deployment is unconfirmed.
- Added opt-in deployment health verification after deployment-service promotion: the runtime can call a separate `deployment_health` HTTPS JSON adapter with the deployed external ref, records health evidence in the promotion result artifact, and leaves the promotion effect `unknown` if the health adapter returns `unhealthy`, `failed`, or no confirmed result.
- Added opt-in non-git deployment rollback orchestration: when a deployment succeeds but health verification fails, the runtime can call a separate `deployment_rollback` HTTPS JSON adapter with the deployed external ref, persist rollback evidence in the promotion result artifact, and still keep the promotion effect `unknown` for operator reconciliation rather than treating rollback as a healthy promotion.
- Added a no-shell local deployment command adapter for self-evolution production promotion: approved promotions can now call configured deployment, health-check, and rollback scripts with reviewed JSON input/output manifests, preserving approval metadata, preview hashes, effect logs, and promotion result artifacts. `config/v3/provider_adapters.local-deployment.example.json` provides the no-secret local command template.
- Added a production provider-readiness audit and CLI: `provider_production_readiness_report` and `agents/super/cli/v3_provider_readiness.py` now verify that resolver/adapter configs exist, required providers are configured, env-backed endpoints and bearer-token vars are actually set, inline secrets are rejected by default, and local command adapters point to executable files before any production dress rehearsal.
- Added guarded no-secret provider config bootstrapping: `v3_provider_readiness --write-missing-templates` now creates missing `data/v3/provider_resolvers.json` and `data/v3/provider_adapters.json` from runtime templates without overwriting existing configs or writing secrets. The workspace has been bootstrapped to the no-secret template state.
- Added no-secret provider provisioning template generation: `v3_provider_readiness --write-env-template` now writes a dotenv-style placeholder file from the active resolver/adapter configs so production endpoint/token provisioning has a concrete checklist without committing real secrets. Readiness also rejects the generated placeholder endpoints/tokens, so sourcing the template unchanged cannot produce a false `ready: true`.
- Added no-secret provider provisioning runbook generation: `provider_provisioning_runbook`, `write_provider_provisioning_runbook`, and `v3_provider_readiness --write-runbook` now render the active readiness findings, required env placeholders, provider-scoped readiness/canary commands, and a recommended first canary section into a Markdown artifact without writing secrets.
- Added approval-gated automatic promotion staging for self-evolution: when `auto_promotion_enabled` is set and prior canary records meet the configured N-run threshold with no real failures, the workflow stages the normal `promote_production` effect for approval. Approval-required records are not treated as canary failures, and the threshold count is capped in the preview so approval hashes remain stable across the approved rerun.
- Added a generic approved provider-effect adapter runner for live side effects: planned Substack, RSS, social, market, and health effects can now be executed through injected or config-backed HTTPS JSON provider adapters only when approval-token and preview-hash metadata are present. Production adapter profiles use env-backed endpoint and bearer-token configuration; successful provider responses mark effects succeeded with external refs, while ambiguous or crashed executions become `unknown` for reconciliation.
- Added a concrete local RSS feed provider adapter: approved `publish_rss` effects can now consume the podcast RSS preview artifact, create or update an RSS feed item by GUID, attach the audio enclosure URL when present, and mark the effect succeeded with an `rss_feed:<path>:<guid>` external ref. `config/v3/provider_adapters.local-rss.example.json` provides the no-secret local feed template.
- Added a concrete hosted RSS HTTPS adapter: approved `publish_rss` effects can now send the reviewed RSS preview to an env-backed HTTPS endpoint, require provider publish confirmation, persist `rss_publish_result.json`, and mark the effect succeeded with the remote episode/feed URL. `config/v3/provider_adapters.hosted-rss.example.json` provides the no-secret hosted RSS template.
- Added a concrete hosted social HTTPS adapter: approved `post_social` effects can now send the reviewed social preview to an env-backed HTTPS endpoint, require provider post confirmation, persist `social_publish_result.json`, and mark the effect succeeded with the remote post URL. `config/v3/provider_adapters.hosted-social.example.json` provides the no-secret hosted social template.
- Added concrete hosted market and health HTTPS adapters: approved `send_market_alert` and `write_health` effects now send reviewed preview payloads to env-backed HTTPS endpoints, require provider confirmation, persist `market_alert_result.json` / `health_write_result.json`, and mark effects succeeded with remote alert/record refs. `config/v3/provider_adapters.hosted-market.example.json` and `config/v3/provider_adapters.hosted-health.example.json` provide no-secret templates.
- Deepened non-RSS provider adapter semantics: social, market, health, and RSS HTTPS adapter profiles now include the approved workflow preview artifact in the provider request body when a `preview_filename` is configured. Social, market, and health previews now carry executable content/message/record fields rather than only targets, and the social workflow has an end-to-end approved-preview HTTP adapter regression.
- Added a local provider-state adapter for non-RSS live-effect rehearsal: approved social, market, health, and Substack effects can now write durable provider-state manifests in the schema the reconciler already trusts. A focused market regression proves the adapter writes approved preview evidence and that a separate planned effect can reconcile from the produced manifest. `config/v3/provider_adapters.local-state.example.json` provides the no-secret local state template.
- Added governed podcast TTS synthesis staging and local command execution: `podcast_production` now writes a `tts_synthesis_preview.json`, requires `external_provider` approval before leaving a planned `synthesize_tts` effect, and supports a `local_tts_command` adapter that invokes a configured local TTS command with reviewed script input and verifies a non-empty audio artifact before marking the effect succeeded. `config/v3/provider_adapters.local-tts.example.json` provides a no-secret local `say` template.
- Added a concrete hosted TTS HTTPS adapter: approved `synthesize_tts` effects can now send reviewed script/voice metadata to an env-backed HTTPS endpoint, accept base64 audio or audio URL responses, write a verified non-empty audio artifact, and persist `tts_synthesis_result.json` with provider evidence. `config/v3/provider_adapters.hosted-tts.example.json` provides the no-secret hosted TTS template.
- Added a local provider dress-rehearsal runner and CLI: `run_local_provider_dress_rehearsal` plus `agents/super/cli/v3_provider_dress_rehearsal.py` now drive social, market, and health canaries through approval request, approval grant, approved preview execution, local provider-state manifest writing, and append-only effect reconciliation.
- Added a readiness-gated production provider canary runner and CLI: `run_provider_production_canary` plus `agents/super/cli/v3_provider_production_canary.py` now refuse to mutate state until selected provider resolver/adapter readiness passes, then run the selected canary through approval, configured provider adapter execution, effect logs, and reconciliation.
- Expanded the production provider canary surface from social/market/health to include Substack publish, podcast RSS publish, and podcast TTS synthesis. The TTS production profile now uses the concrete `hosted_tts_http` adapter rather than the generic JSON adapter, so a TTS canary proves reviewed-script synthesis semantics and audio artifact handling.
- Fixed provider readiness scoping so adapter-only canaries such as TTS can require no resolver without accidentally expanding back to every resolver profile.
- Added a dashboard-visible production canary-surface verifier: `v3_status --json` now proves six selected-provider canaries are locally supported (`substack`, `rss`, `tts`, `social`, `market`, `health`), with five resolver-backed providers and TTS correctly marked adapter-only.
- Added a weekly north-star report generator and CLI: `build_weekly_north_star_report`, `write_weekly_north_star_report`, and `agents/super/cli/v3_north_star_report.py` now turn the durable ledger, commit log, effect log, and causal evidence log into a persisted markdown report matching the V3.1 §23 review shape. Reports now use a bounded week window with previous-window comparisons instead of silently scoring the entire ledger, support `--output-dir` for operator-selected artifact locations, and the causal-memory score now uses V3.1's matching-scar opportunity denominator while still reporting raw causal-link coverage.
- Added the V3.1 §22.8 `CausalTrace` model and important-behavior trace builder. The weekly report now distinguishes structural trace completeness for important behavior from the separate causal-memory metric that measures whether prior memory actually changed behavior.
- Added a live Causal Trace implementation-status verifier that exercises the L1/L3/L4 evidence-level contract, ablation promotion rules, required trace anchors for public behavior, and live durable trace completeness / L4 ablation-reference integrity.
- Added a live Provider Effect Adapter implementation-status verifier that exercises approval-bound adapter execution for Substack, RSS, TTS, social, market, and health effects; verifies succeeded effects are reused idempotently; verifies missing approval metadata blocks execution; verifies ambiguous and failed adapter results become `unknown` and `failed`; checks env-backed no-inline-secret adapter config shape; and scans live provider effects for succeeded rows missing approval metadata.
- Extended the Provider Effect Adapter status verifier to cover all nine configured production adapters by exercising self-evolution deployment, deployment-health, and deployment-rollback adapters in a disposable git repository, including the unhealthy-deployment rollback path.
- Added a live Web Review Queues implementation-status verifier that synthesizes approval, memory-commit, experiment, incident, effect-reconciliation, briefing-feedback, public-writeup-review, public-feedback-followup, customer-discovery feedback, and provider-provisioning queue items; verifies each queue carries the required §16 one-screen review context; and checks queue-specific metadata such as preview hashes, expiry, gateway findings, experiment windows, replay refs, briefing feedback button refs, public-writeup draft review refs, public-feedback stats/commands, customer-discovery packet/record commands, and provider readiness commands.
- Added a live Legacy Runtime Bridge implementation-status verifier that checks legacy task/background routing, durable V3 task and background records, failure-scar capture, approval/preflight gate handling without failure-memory writes, background snapshot export, and detached post-hook wiring to `record_task_completion`.
- Fixed legacy background snapshot export after failure-memory records: `to_jsonable` now recurses through tuples, so snapshots containing scars serialize cleanly.
- Expanded dashboard/status scorecard observability: `v3_status --json` now includes repeated-error, causal-memory, output-quality, memory-health, self-evolution, approval-safety, traceability, and per-metric detail fields instead of only aggregate score plus hard gates.
- Replaced placeholder voice and briefing-interest baseline fields with artifact-backed deterministic MVP metrics. The weekly report now scores readable article/social artifacts for voice stability and trust-labeled briefing artifacts for interest fit, while leaving the metrics unmeasured when no artifacts exist.
- Added concrete A2A trust public-writeup draft, commercial-option, and product-thesis evidence: the A2A workflow now writes `a2a_public_writeup_draft.md`, `a2a_commercial_options.md`, and `a2a_product_thesis.md`, emits explicit tool, feedback-plan, public-writeup-plan, product-thesis, and commercial eval refs, and dashboard/status exposes `commercial_options` plus product-thesis updates while keeping public writeups at 0 until actual publication.
- Added dashboard-visible public-writeup review queue coverage: the latest A2A draft now appears as `public_writeup_review` with title, draft artifact, preview hash, `public_writeup:<slug>:url=<url>` and `external_feedback:<slug>:source=<source>` templates, and `needs_publication_review` state, so the next human action toward publication and feedback is visible without inflating strategic counters.
- Added hash-bound public evidence command templates to the dashboard public-writeup review queue: each draft now includes a `v3_record_public_evidence.py` command template with `--draft-artifact`, `--expected-preview-hash`, `<url>`, and `<source>` placeholders, reducing the chance that post-publication evidence is recorded against the wrong draft.
- Added a dashboard-visible provider-provisioning queue: blocked production provider readiness now exposes missing env counts, config/runbook paths, full readiness/runbook command templates, and a scoped provider readiness/canary command pair without claiming external credentials are present.
- Refined provider-provisioning next actions to choose the smallest configured production canary scope from the live readiness findings. In the current workspace, the dashboard and weekly report now surface TTS as the first canary candidate because it is adapter-only and blocked on two env vars (`MIRA_TTS_ADAPTER_ENDPOINT`, `MIRA_TTS_ADAPTER_TOKEN`) instead of a resolver-plus-adapter provider with four env vars.
- Added dashboard-backed next actions to the weekly north-star report: generated reports now carry the hash-bound public-writeup evidence command, external-feedback ref template, provider provisioning runbook path, readiness command, and scoped canary command from the live review queues.
- Added deterministic public-writeup safety auditing: `public_writeup_safety_report` and `v3_public_writeup_safety.py` flag private local paths, credential-like material, unresolved placeholders, and editorial markers; dashboard public-writeup review items expose the audit status/command, and public evidence recording rejects unsafe draft artifacts before counting publication refs.
- Added publication-packet preparation for the A2A public writeup: `prepare_public_writeup_publication_packet` and `v3_prepare_public_writeup_packet.py` create a local packet with submission Markdown, metadata JSON, safety report, feedback ref template, and the hash-bound evidence-recording command. The live packet for `a2a_manifest_note` is under `data/v3/artifacts/publication_packets/a2a_manifest_note/1b9fcd57bb45/`.
- Added feedback solicitation packets for recorded public writeups: `prepare_public_feedback_solicitation_packet` and `v3_prepare_public_feedback_packet.py` create a local request Markdown, metadata JSON, checklist, publication stats snapshot, and feedback-only recording command without counting feedback. The live packet for `v31_green_dot_is_not_evidence` is under `data/v3/artifacts/public_feedback_packets/v31_green_dot_is_not_evidence/136a794141c7/`, and the dashboard/report now expose the packet command before the feedback-recording command.
- Surfaced provider env-template provisioning in the dashboard and weekly report: provider-provisioning queue items now include `data/v3/provider_provisioning.template` plus the exact `v3_provider_readiness.py --write-env-template ... --overwrite-env-template --json` command, and the live template/runbook were regenerated.
- Added scoped provider env-template provisioning for the smallest current canary. The dashboard and weekly report now expose a scoped `v3_provider_readiness.py --write-env-template ... --skip-resolvers --require-adapter tts --json` command, and the live `data/v3/provider_provisioning.tts.template` contains only `MIRA_TTS_ADAPTER_ENDPOINT` and `MIRA_TTS_ADAPTER_TOKEN` placeholders.
- Added validated public evidence ingestion: `record_public_writeup_evidence()` and `v3_record_public_evidence.py` append durable operator-supplied public-writeup and optional external-feedback refs only after concrete URLs/sources pass validation and an optional local draft artifact matches its expected sha256 preview hash. The strategic scorecard now excludes `operator_evidence` records from A2A experiment counts, so post-publication evidence can move public-writeup/feedback metrics without pretending a new experiment ran.
- Added feedback-only public evidence ingestion: `record_public_feedback_evidence()` and `v3_record_public_feedback.py` append `external_feedback:*` refs for an already recorded public writeup without duplicating the `public_writeup:*` count. The recorder now rejects orphan feedback when the writeup slug has not been recorded and rejects mismatched published URLs; the strategic scorecard also ignores orphan publication-feedback refs while still counting independent `customer_discovery:*` feedback.
- Added A2A trust hypothesis evidence accumulation: each A2A experiment now updates `hypothesis:a2a_trust_manifest` with a durable evidence-for entry, so the dashboard experiment queue no longer shows an empty hypothesis after successful runs.
- Corrected repeated-error accounting so approval-required safety gates are reported separately instead of being counted as failed runs merely because the ledger stores an approval explanation in `what_failed`.
- Added a weekly causal coverage gap breakdown that only counts anchored records needing review, separating actionable causal-link gaps from routine scheduled trace records.
- Added a weekly failure-mode breakdown and durable task failure signatures: failed task completions now update `failure:<pipeline>:<mode>` kernel signatures, and the north-star report groups failed runs by actionable mode rather than leaving the communication failure cluster opaque.
- Refined weekly causal-gap accounting so failure records that captured a scar or failure signature are counted as failure-memory coverage instead of false missing-causal-link gaps, and first-run baseline artifacts are reported separately from actual causal coverage gaps.
- Refined weekly causal-gap accounting again so operator-supplied evidence ingestion records, such as `public_writeup:*` evidence recordings, are not reported as missing causal traces for Mira behavior. The live weekly report now shows `Causal Coverage Gaps - none detected`.
- Refined the weekly `New Experiments` section so operator-supplied evidence ingestion records no longer appear as new A2A experiments. The live weekly report now excludes `a2a_public_evidence_57fcb2618fb3` from that section while preserving actual A2A experiment runs and strategic refs.
- Completed the missing V3.1 Eval 6 report shape: the weekly north-star report now pairs approval burden with side-effect throughput, side-effect incident rate, near-miss rate, unapproved high-risk actions, rollback count, and published-item throughput instead of reporting approval safety alone.
- Added a V3.1 `ApprovalEvent` view over the append-only approval log: approved, edited, denied, expired, and pending approval requests now expose requested/resolved timestamps, decisions, action/risk identity, and human-minute timing for weekly burden metrics while preserving the existing request/grant flow. Edited decisions are now first-class append-only resolutions, and the event view uses the latest resolution row so post-grant edits are reflected in burden metrics.
- Added a V3.1 `IncidentEvent` view over the durable ledger and effect log: failed/unknown/reconciled-failed effects, unapproved high-risk effects, and failed experience records now expose incident id, run id, severity, optional action id, approval status, root cause, and preventability. `RunEvalBundle` now carries these incident events, and the weekly Eval 6 verdict fails when explicit incident rate rises or any high/critical incident is present. The weekly report now includes an incident breakdown by root cause, high/critical count, and preventable count.
- Corrected Eval 6 incident classification so expected human publish confirmations and `PREFLIGHT BLOCKED` guardrail records are counted as approval/near-miss gates, not incidents. Verification-related `needs-input`, true `preflight failed`, provider timeout, missing-source, and no-verifiable-output records still remain incidents.
- Fixed the legacy bridge so future human gates and preflight guardrail completions write gate outcomes without creating scars or failure signatures. True task failures still create failure-memory signatures.
- Added test isolation for legacy post-hook V3 task recording and live weekly scorecard filtering for known test task records.
- Added approval queue capacity controls for the V3.1 review budget: approval requests now carry 24-hour expiry metadata, overdue requests default to expired/no before grants can be issued, dashboard approval items expose expiry, age, evidence, rollback, and budget throttle fields, and the executor blocks new non-critical public/code/memory approval requests while the queue is over budget unless the payload is explicitly marked critical.
- Added the §16.6 low-risk approval digest: pending preview-bound public approvals are grouped into a non-mutating `approval_digest` review item with request ids, preview hashes, expiry, estimated review minutes, and required one-screen context while each underlying request still requires an explicit decision.
- Added required V3.1 review-question context across all dashboard queues: approval, memory commit, experiment, incident/DLQ, and effect reconciliation items now answer why now, what will change, what evidence supports it, what can go wrong, and how to roll back.
- Added missing V3.1 memory-commit queue metadata: gateway commits now preserve source trust, memory class, risk level, privacy tier, evidence refs, and contradiction summaries, and the dashboard memory queue exposes those fields with the available operator decisions.
- Added missing V3.1 experiment queue controls: hypotheses now persist optional baseline window, test window, `min_n`, current metric, and rollback plan from memory-action metadata; the self-evolution workflow seeds those fields, and the dashboard backfills reviewable defaults for legacy hypotheses.
- Added a first-class V3.1 experiment registry: ledger evidence now derives `ExperimentRecord` rows and the weekly north-star report now reports Eval 5 coverage, testability, conclusion, rollback, untracked-change, missing-rollback, and governed-risk metrics.
- Added V3.1 per-run eval and outcome rows: `RunEvalBundle` now carries `EvalRecord` and `OutcomeRecord` lists derived from durable experience records, the weekly north-star report counts them, and the dashboard exposes eval/outcome row counts in the operational scorecard.
- Added missing V3.1 decision/effect/approval bundle rows: `RunEvalBundle` now carries `DecisionRecord`, `BehavioralEffect`, and `ApprovalEvent` evidence derived from ledger causal links, causal-evidence rows, and append-only approval events; the weekly report counts them and the dashboard exposes decision/effect/approval row counts.
- Added V3.1 per-run evidence bundles under the aggregate operational scorecard: each durable experience now has a run-level bundle with run id, pipeline/workflow, timestamp, intent, expected and actual outcomes, snapshot id, retrieved/included memory ids, decision/effect/eval/outcome/approval/incident/failure rows, memory proposal/commit ids, and causal links.
- Added missing V3.1 Eval 4 briefing feedback coverage: briefing items now get stable review ids, topic/interest matching, user button outcomes, promotion/dismissal rates, interest coverage, novel-but-relevant metrics, and a deterministic weekly blind-sample queue for unreviewed briefing items.
- Added the Eval 4 briefing feedback queue to the dashboard: weekly blind-sample briefing items now expose item ids, topics, matched interests, novelty/actionability scores, button-ref templates, recorder command templates, and V3.1 button aliases (`useful`, `too_obvious`, `surprising`, `wrong`, `follow_up`, `pursue_research`, `pursue_article`, `not_useful`) so human interest feedback can enter the durable eval refs.
- Added `v3_record_briefing_feedback.py`, backed by runtime validation that rejects placeholders, unknown buttons, and item ids not present in the current weekly blind-sample queue before appending operator-supplied `briefing_feedback:*` evidence.
- Tightened Eval 4 plan fidelity: action/dismiss/novel-but-relevant rates now use total scored briefing items as denominator, `briefing_interest_eval.py` returns `passed: false` until real briefing feedback exists, and the weekly report shows `WATCH` instead of a false PASS when `feedback_items` is zero.
- Fixed the strategic north-star score formula to match §23.4's normalized weights: A2A research progress, reproducible experiments, tool/public artifacts, public feedback, product thesis, and revenue/commercial options now sum to 1.0, and external feedback is capped at the plan's three-event monthly standard. The live strategic score is now 85/100 while external feedback remains 0, rather than a false 100/100.
- Added top-level weekly report Watch Gates for incomplete but non-hard-failing north-star evidence: missing external feedback, missing briefing operator feedback, and blocked provider production readiness now appear in the summary before the detailed next-action commands.
- Added missing V3.1 Eval 7 memory audit coverage: gateway commit evidence now derives `MemoryAuditRecord` rows with source/trust/privacy/evidence metadata, valid/unsupported/false/stale/unsafe/duplicative verdicts, action-taken semantics, memory precision, unsupported-claim rate, quarantine recall, critical pollution, and snapshot contamination metrics in the weekly north-star report.
- Added the missing V3.1 poisoning red-team harness: `run_poisoning_redteam` plus `agents/super/cli/v3_poisoning_redteam.py` now exercise prompt-injection, secret material, untrusted source, approval-bypass, unsupported causal claim, duplicate, contradiction, unsupported hypothesis, high-risk kernel change, and evidence-backed benign cases. The first run exposed that generic high-risk `update_relationship` proposals were auto-applied; the gateway now requires human review for all high/critical memory-risk proposals.
- Added missing V3.1 Eval 1 failure-signature recurrence coverage: durable ledger records now derive `FailureEvent` rows and per-signature `FailureSignatureEval` summaries with first-seen/scar-created timestamps, before/after opportunities, post-scar recurrence rate, scar-prevention rate, and high-severity repeat-failure counts in the weekly north-star report.
- Added the missing §24 standalone eval entry points: `repeated_error_eval.py`, `causal_memory_eval.py`, `trace_completeness_eval.py`, `voice_eval.py`, `briefing_interest_eval.py`, `memory_audit.py`, and `approval_safety_eval.py`. Each CLI reuses the existing V3.1 eval engine, emits JSON, supports bounded weekly windows, and can limit run/effect evidence to the first-stage workflow scope from the plan.
- Added explicit first-stage-scope support to the weekly north-star report path: `v3_north_star_report --first-stage-scope` now evaluates only Podcast/System Health/Incident, Article, Briefing, and A2A Trust records/effects while reporting how many non-synthetic records were excluded by that scope.
- Tightened the strategic north-star scorecard so generic `strategic:*` markers and feedback plans no longer count as tool prototypes or external feedback events. Dashboard/status/report output now separates public writeups, actual external/public feedback, product-thesis updates, and commercial options, exposing the current local gap instead of inflating strategic progress.
- Reworked Phase -1 baseline capture so `capture_all_baselines` derives repeat-error, voice, briefing-interest, approval-burden, memory-audit, and trace-completeness fields from the V3.1 eval engine, excludes known synthetic task fixtures, and uses persisted causal evidence for causal validity.
- Added `v3_baseline_capture.py` and captured live 2026-05-21 baseline artifacts under `data/v3/baselines/`.
- Added a dashboard-visible V3.1 implementation status matrix with linked evidence and tests for Ledger, Memory Gateway, Capability Preflight, Effect Log, Provider Effect Adapters, Workflow Packs, Approval Queue, Causal Trace, Snapshot Builder, North Star Evals, Baselines, Web Review Queues, Legacy Runtime Bridge, and Provider Production Readiness. Each row now carries the matching V3.1 plan reference, missing-evidence/test lists, verifier checks where available, and a status detail. The Ledger row verifies durable ExperienceRecord JSONL rows, required record fields, parser/schema consistency, and unique run ids. The Memory Gateway row runs the deterministic poisoning red-team harness and validates live memory commits for structured findings, blocked writes, and high/critical risk applied commits. The Capability Preflight row verifies connector requirements carry scopes/risk metadata and that representative missing connectors degrade with visible fallbacks or block fail-closed. The Provider Effect Adapters row verifies approval-bound adapter execution, idempotent succeeded-effect reuse, missing-approval blocking, unknown/failed adapter transitions, deployment/health/rollback adapter orchestration, env-backed adapter config shape, and live succeeded-effect approval metadata. The Approval Queue row verifies action-risk policy, request deduplication by preview hash, preview-bound grants, changed-preview invalidation, expiry/no-late-grant behavior, over-budget auto-pause pressure, approval event decisions, and live unexpired grant safety. The Causal Trace row verifies L1/L3/L4 evidence classification, ablation promotion, required trace anchors, live trace completeness, and live L4 ablation references. The Snapshot Builder row builds creative and bodily snapshots and verifies manifest identity, item score coverage, local-only privacy exclusion, bodily inclusion, token accounting, and recent same-pipeline experience inclusion. The Effect Log row verifies durable append-only effect rows, required fields, valid statuses, unique effect ids, and execution timestamps on latest succeeded states. The Workflow Packs row now verifies workflow-tree security, runtime registry coverage for 20/20 non-native workflow packs, compile-time name/memory-class consistency, and the briefing source-bundle contract. The Web Review Queues row verifies approval, approval digest, memory, experiment, incident, effect, briefing-feedback, public-writeup, public-feedback, customer-discovery, and provider-provisioning queue context plus queue-specific metadata. The Legacy Runtime Bridge row verifies task/background routing, durable records, gate-safe memory behavior, background snapshot export, and post-hook wiring. The Baselines row verifies the latest complete six-artifact Phase -1 baseline set and required JSON fields. The North Star Evals row runs a live non-mutating operational/strategic eval gate over durable ledger, commit, effect, causal, and approval stores, proves the §14.4 eval-threshold rule blocks one-anecdote changes, over-large deltas, golden-set regressions, and public/health/compliance-impacting changes without approval, and now reports `feedback_integrity=PASS` after checking orphan/mismatched publication feedback. The production readiness row runs the same provider readiness verifier used by `v3_provider_readiness.py`, remains `blocked_external` while endpoint/token env vars are missing, and can move to `verified` when that check passes.
- Added a repository-wide workflow security audit entry point: `v3_workflow_security_audit.py` checks every workflow command and skill file under `workflow_packs/`, including unreferenced skill directories, and fails if any newly generated or imported skill contains suspicious shell/network/code-execution, privacy-downgrade, undeclared live-tool, orphaned-risk/effect, or memory-write-without-evidence patterns.
- Added an in-memory runtime audit gate for generated/imported skill candidates: `skill_learning` now audits candidate `skill.yaml`/`SKILL.md` content before any save or enablement path, blocks suspicious network requests, shell/code execution, privilege escalation, obfuscation, and credential-store access, and writes a non-enabling audit artifact for review.
- Added executable §17.2 source-bundle discipline for `intelligence_briefing`: the fetch step writes `source_fetch_records.json` with stable source ids, source type, trust/privacy tiers, evidence refs, fetched timestamp, and content hash; the dedup/trust step writes `source_bundle.json` with deduped sources, duplicate count, and trust summary; the writer consumes the deduped source bundle rather than raw source rows.
- Strengthened the §13 snapshot builder contract: `SnapshotManifest` now records run id, profile, total token estimate, included/excluded ids, exclusion reasons, item scores, and a `snapshot_hash` alias; `SnapshotItem` now carries memory id, inclusion rationale, trust/privacy tier, and the full V3.1 scoring-signal breakdown. Pipeline execution passes the real run id into snapshot manifests.
- Strengthened the §9 connector-aware preflight contract: capability checks now expose status/scopes/risk tier/last-checked/fallback metadata, preflight plans report missing optional connectors and degradation notes, degraded successful runs persist `preflight_degraded:*` refs, and blocked runs persist `preflight_missing:*` refs.
- Closed the §15 replay-bundle gap for effect actions: the executor now writes a redacted JSON replay bundle before executing an effect step, stores its path on the append-only effect log, preserves it through status transitions/reconciliation, includes a compensation strategy, and exposes the bundle ref in the effect reconciliation queue.
- Closed the §15 side-effect checkpoint gap: checkpoints now carry a backward-compatible phase, effect steps force a `before_effect` checkpoint before tool execution and an `after_step` checkpoint afterward, and resume only skips a step from completed checkpoints.
- Added the missing §22.8 / §24 replayability enforcement: the operational scorecard now computes `unreplayable_action_rate`, fails a hard gate when public/memory/code effects lack replay bundles, validates inspectable local replay-bundle JSON structure and idempotency-key binding, and reports missing plus invalid replay bundles against important effects.
- Added append-only legacy replay-bundle recovery: `v3_replay_bundle_recovery.py` reconstructs bundle payloads from existing checkpoints, artifacts, and provider-state manifests, writes recovered bundles under `data/v3/effect_replay_bundles/`, and appends effect-log rows with recovered `replay_bundle_ref` values instead of mutating historical rows.

## Verification

- `tests/v3`: 312 passed.
- Focused scoped provider env-template/canary queue/report/runbook regressions: 5 passed.
- Focused status-matrix public-feedback integrity regressions: 14 passed.
- Focused orphan public-feedback guardrail regressions: 16 passed.
- Focused public-feedback packet/dashboard/report regressions: 14 passed.
- Focused replay recovery/effect-log/north-star regressions: 49 passed.
- Focused north-star replayability gate regressions: 43 passed.
- Focused side-effect checkpoint/resume regressions: 15 passed.
- Focused §15 replay-bundle/effect-log/dashboard regressions: 20 passed.
- Focused baseline/dashboard status matrix coverage: 8 passed.
- Focused snapshot builder/governance/memory-first regressions: 15 passed.
- Focused capability preflight/executor/router regressions: 16 passed.
- `v3_baseline_capture --date 2026-05-21 --json`: wrote the six Phase -1 baseline artifacts for `2026_05_21`.
- Live baseline capture for 2026-05-21: operational repeat-error rate 0.0000, incident rate 0.00 per 100 side effects, voice mean 0.8545 over 11 samples, briefing precision@5 1.0000 over 10 scored items, briefing feedback coverage 0.0000, approval requests 25.64 per 100 side effects with 0 unapproved high-risk actions, memory precision 0.9693, trace completeness 1.0000, causal-link validity 1.0000, and 149 synthetic fixture records excluded.
- Focused §24 eval checklist and first-stage north-star scope coverage: 40 passed.
- `repeated_error_eval --week 2026-05-21 --json`: passed with score 1.0000, 0 failed runs, and 0 high-severity repeat failures.
- `causal_memory_eval --week 2026-05-21 --json`: passed with causal-memory score 1.0000 and causal-link validity 1.0000.
- `trace_completeness_eval --week 2026-05-21 --json`: passed with traceability 1.0000, 2106 important behavior traces, and 0 traces below 95% completeness.
- `voice_eval --week 2026-05-21 --json`: passed on 11 artifact-backed samples with mean voice score 0.8545 and generic-failure rate 0.0909.
- `briefing_interest_eval --week 2026-05-21 --json`: now returns `passed: false` on 5 briefing samples / 10 scored items because `feedback_items: 0`; this is the correct non-passing state until operator feedback is recorded through the blind-sample queue.
- `memory_audit --week 2026-05-21 --json`: passed across 2245 audited memories with precision 0.9693 and critical pollution 0.
- `approval_safety_eval --week 2026-05-21 --json`: passed with approval-safety score 1.0000, 10 approval events, 0 incidents, and 0 unapproved high-risk actions.
- `causal_memory_eval --week 2026-05-21 --first-stage-scope --json`: passed with 365 first-stage records and 1 changed-strategy run from 1 scar opportunity.
- `v3_north_star_report --week 2026-05-21 --first-stage-scope --stdout`: passed with 374 current first-stage records, 1744 current non-synthetic records excluded by scope, hard gates passing, north-star score 100/100, and strategic score 85/100 because external feedback remains 0.
- Focused north-star/dashboard V3.1 row and Eval 4 feedback regressions: 43 passed.
- Focused poisoning red-team/gateway/governance regressions: 25 passed.
- `v3_poisoning_redteam --json`: passed 10 / 10 deterministic memory-poisoning cases with 0 critical failures.
- Focused causal evidence/governance regressions: 15 passed.
- Focused north-star Eval 1/Eval 4/Eval 7 regressions: 36 passed.
- Focused briefing feedback / Eval 4 regressions: 34 passed.
- Focused briefing feedback recorder/dashboard/status regressions: 5 passed.
- Focused EvalRecord/OutcomeRecord dashboard/report regressions: 37 passed.
- Focused experiment registry/north-star report regressions: 33 passed.
- Focused experiment queue metadata/kernel/workflow regressions: 25 passed.
- Focused dashboard review-question regressions: 7 passed.
- Focused memory-commit queue/gateway regressions: 27 passed.
- Focused approval capacity/dashboard/executor regressions: 24 passed.
- Focused approval digest/dashboard/status regressions: 21 passed.
- Focused approval/effect queue regression checks: 10 passed.
- Focused north-star eval/dashboard/strategic workflow regressions: 26 passed.
- Focused strategic scorecard/dashboard/workflow regressions: 154 passed.
- Focused causal trace/evidence regressions: 4 passed.
- Focused runtime router regressions: 3 passed.
- Focused runtime/workflow-pack regressions: 34 passed.
- Focused podcast routing/TTS/RSS staging/publication regressions: 10 passed.
- Focused social publish staging/provider regressions: 6 passed.
- Focused market/health external-write staging/provider-state/live-adapter regressions: 7 passed.
- Focused memory compaction staging/adapter regressions: 7 passed.
- Focused self-evolution branch canary regressions: 2 passed.
- Focused self-evolution production promotion/canary/deployment regressions: 8 passed.
- Focused effect/provider/runtime resolver regressions: 19 passed.
- Focused provider adapter execution/config/readiness/canary regressions: 81 passed.
- Focused local provider dress rehearsal regressions: 2 passed.
- Focused causal runtime regressions for L3/L4 workflow evidence: 2 passed.
- Focused gateway/governance regressions: 20 passed.
- Workflow-pack security regressions: 17 passed.
- `v3_workflow_security_audit --json`: passed across 62 checked workflow command/skill files with 0 findings.
- `v3_replay_bundle_recovery --json`: recovered 4 legacy replay bundles from durable checkpoints, artifacts, and provider-state manifests; all recovered rows reported `payload_reconstructable: true`.
- `v3_status --json`: strategic and operational scorecards now have no hard gate failures again after recovered replay bundle refs were appended for the legacy rows and local replay bundles validated.
- `v3_status`: the human-readable summary now exposes operational score, strategic score, the current north-star watch gates, and implementation blockers, so operators no longer need JSON parsing to see `external_feedback_below_standard:0/3`, `briefing_feedback_missing`, and `provider_production_readiness_blocked`.
- Added a remaining-gates handoff contract test that extracts CLI commands from `docs/v31-north-star-remaining-gates-2026-05-21.md`, verifies each referenced script exists, and checks that each documented flag appears in the script's `--help` output.
- Added `v3_remaining_gates.py`, a live dashboard-backed remaining-gates handoff generator, and regenerated `docs/v31-north-star-remaining-gates-2026-05-21.md` from current queues so feedback item ids, publication commands, provider env vars, and implementation blockers do not drift from runtime state.
- `v3_status --json`: operational scorecard now exposes component metrics and direct hard-gate counters; latest live snapshot reports operational score 0.9882, repeated-error score 0.9980, causal-memory score 0.9444 over the recent dashboard window, `unreplayable_action` 0, `invalid_replay_bundle` 0, `unreplayable_action_rate` 0.0000 across 4 important effects, `replay_bundle_validity` 1.0000 across 4 replay bundle refs, eval/outcome/decision/run-evidence row counts 500 / 500 / 500 / 500 plus 62 behavioral effects and 0 approval events for the dashboard window, approval capacity pending 0 / budget 10 with 15-minute daily budget and no auto-pause pressure, strategic score 0.8500 with `experiments:8`, `artifacts:17`, `tools:4`, `writeups:1`, `external_feedback:0`, `product_thesis:2`, and `commercial:8`, dashboard strategic `watch_gates` carrying the external-feedback, briefing-feedback, and provider-production-readiness blockers as the report/status surfaces, A2A hypothesis evidence-for count 3, 14 implementation status rows with direct V3.1 plan refs, a passing `experience_ledger_records` verifier check, a passing `memory_gateway_contract` verifier check, a passing `capability_preflight_contract` verifier check, a passing `provider_effect_adapter_contract` verifier check with `exercised=9` and `deployment_exercised=3`, a passing `approval_queue_contract` verifier check, a passing `causal_trace_contract` verifier check, a passing `snapshot_builder_contract` verifier check, a passing `effect_log_integrity` verifier check, a passing `web_review_queue_contract` verifier check with `public_writeup_review:1`, `public_feedback_followup:1`, `context_complete=10/10`, and `queue_specific_checks=10/10`, a passing `legacy_runtime_bridge_contract` verifier check, a passing `workflow_tree_security_audit` verifier check with `candidate_gate_blocked=1`, a passing `workflow_pack_registry_coverage` verifier check with `registered=20/20`, `compiled=20`, and `native=communication`, a passing `briefing_source_bundle_contract` verifier check with `source_records=3`, `deduped_sources=2`, and `duplicate_count=1`, a passing `baseline_artifact_set` verifier check, a passing `north_star_eval_gate` verifier check with `threshold_policy=PASS`, `threshold_violations=0`, and watch gates for `external_feedback_below_standard:0/3`, `briefing_feedback_missing`, and `provider_production_readiness_blocked`, a passing `provider_production_canary_surface` check with `canary_supported=6/6`, a failing `provider_production_readiness` check that names the missing endpoint/token provisioning surface, and no missing local evidence/tests except the intentionally external production readiness gate, experiment queue items with baseline/test windows, `min_n`, current metric, rollback plan, and required one-screen review context, a `briefing_feedback` queue with 5 weekly blind-sample items and concrete button-ref templates, a `public_writeup_review` queue item for the A2A draft, and a `public_feedback_followup` queue item for `v31_green_dot_is_not_evidence` with current publication stats showing 0 comments, 0 likes, and 0 restacks.
- `v3_provider_readiness --write-missing-templates --json`: created no-secret `data/v3/provider_resolvers.json` and `data/v3/provider_adapters.json`.
- `v3_provider_readiness --write-env-template data/v3/provider_provisioning.template --overwrite-env-template --json`: wrote a no-secret provider provisioning template listing the currently required resolver/adapter endpoint and token env vars with placeholder values only.
- `v3_provider_readiness --write-env-template data/v3/provider_provisioning.tts.template --overwrite-env-template --skip-resolvers --require-adapter tts --json`: wrote a no-secret scoped first-canary template containing only the two TTS adapter env vars; the command correctly remains nonzero until real endpoint/token values are provisioned.
- `v3_provider_readiness --write-runbook data/v3/provider_provisioning.runbook.md --overwrite-runbook --json`: wrote a no-secret Markdown provider provisioning runbook with current findings, required env placeholders, scoped readiness/canary commands for health, market, RSS, social, Substack, and TTS, and a `Recommended First Canary` section for TTS.
- `v3_provider_readiness --json`: currently reports `ready: false` with all required providers configured, and now blocks on unset endpoint/token environment variables or unreplaced placeholder values for resolver and adapter providers. This is the correct blocking state before production dress rehearsal.
- `v3_provider_readiness --skip-resolvers --require-adapter tts --json`: currently reports `ready: false` with resolver findings empty and only the selected TTS adapter env vars blocking. This is the correct adapter-only readiness scope before a credentialed TTS canary.
- `v3_provider_dress_rehearsal --json`: reports `ready: true` for local social, market, and health canaries; each canary has a distinct planned effect id, reconciled append-only effect id, approval grant id, local provider-state manifest, and successful provider-state reconciliation.
- `v3_provider_production_canary --provider social --json`: currently reports `ready: false` without mutating state, scoped to the selected social resolver/adapter env vars that remain unset. This is the correct blocking state before a one-provider credentialed canary.
- `v3_provider_production_canary --provider tts --json`: currently reports `ready: false` without mutating state, scoped to the selected TTS adapter env vars and no resolver requirement. This is the correct blocking state for an adapter-only credentialed canary.
- `v3_prepare_public_feedback_packet --slug v31_green_dot_is_not_evidence --published-url https://uncountablemira.substack.com/p/198208037 --stats-artifact data/social/publication_stats.json --json`: wrote `data/v3/artifacts/public_feedback_packets/v31_green_dot_is_not_evidence/136a794141c7/` with request, checklist, metadata, stats snapshot, and the feedback-only recorder command.
- `v3_north_star_report --week 2026-05-21 --json`: refreshed `data/v3/artifacts/north_star_reports/north-star-week-2026-05-21.md` for the bounded 2026-05-15 to 2026-05-22 window; the operational north-star score remains 100/100 with hard gates passing, the strategic score is now 85/100 with 1 public writeup, 0 external feedback events, 2 product-thesis updates, and 8 commercial options, and the next action includes both the feedback-packet command and feedback-only recorder command for the published writeup.
- Clarified the weekly report's approval-burden publication metric as `publication_side_effects_completed_per_week`, so reconciled social/RSS/Substack side effects are not confused with the strategic `public_writeups` counter.
- Tightened the broader module self-audit: hidden runtime directories such as `.bg_pids` are no longer treated as agent directories, and publish-manifest errors now distinguish active critical publish-review failures from intentionally parked rows and stale historical errors on already-published articles.
- Restored legacy-mode `do_talk` execution under offline Claude/API conditions and made Substack publish audit-context injection backward-compatible with older/test publish callables, without relaxing runtime output verification thresholds.
- Added a no-network briefing feedback packet generator, exposed it in the dashboard and remaining-gates handoff, and prepared the live first-item packet under `data/v3/artifacts/briefing_feedback_packets/889102d9d57a/` so operator feedback can be recorded without guessing item context or button names.
- Added independent customer-discovery feedback capture for the external-feedback gate: `v3_prepare_customer_discovery_packet.py` writes a no-network request/checklist/metadata bundle, `v3_record_customer_discovery_feedback.py` records validated `customer_discovery:*` refs, and the live `a2a_trust_manifest` packet now lives under `data/v3/artifacts/customer_discovery_packets/a2a_trust_manifest/6ee9815b4bcb/`.
- Tightened the Web Review Queues implementation-status verifier so it now fails if briefing feedback queue items lose their item text or packet command, or if the live remaining-gates handoff loses the customer-discovery, briefing packet, effect-inspection, or action-status commands.
- Added a non-mutating provider production canary dry-run path: `v3_provider_production_canary.py --dry-run` now checks readiness and renders the planned provider/workflow/effect surface without creating approval requests, effect-log rows, or provider calls. The provider provisioning runbook and remaining-gates handoff now insert this dry-run step before the mutating canary command.
- Added a CLI-level provider canary dry-run regression proving `v3_provider_production_canary.py --dry-run` succeeds after scoped TTS readiness and creates no approval requests or effect-log rows.
- Added bulk briefing-feedback packet preparation: `v3_prepare_briefing_feedback_packet.py --all --json` now writes review packets for every current weekly blind-sample item, the remaining-gates handoff exposes the command, and the live run prepared 5 packet directories under `data/v3/artifacts/briefing_feedback_packets/`.
- Added bulk public-feedback packet preparation: `v3_prepare_public_feedback_packet.py --all --json` now derives recorded public writeups still missing feedback, skips slugs with existing feedback refs, writes solicitation packets for each pending writeup, and the remaining-gates handoff exposes the command. The live run prepared the pending `v31_green_dot_is_not_evidence` packet.
- Added packet-based public feedback recording: `v3_record_public_feedback.py --packet <feedback_packet.json> --feedback-source <source> --json` now reads slug and published URL from a prepared packet, and regenerated public-feedback packets include this safer command alongside the explicit slug/URL command.
- Added packet-based briefing feedback recording: `v3_record_briefing_feedback.py --packet <briefing_feedback_packet.json> --button <button> --json` now reads the weekly blind-sample item id from a prepared packet, and regenerated all 5 live briefing-feedback packets include this safer command alongside the explicit item-id command.
- Added packet-based customer-discovery feedback recording: `v3_record_customer_discovery_feedback.py --packet <customer_discovery_packet.json> --source <source> --insight <insight> --json` now carries the packet topic/question into the evidence artifact, and the live `a2a_trust_manifest` packet includes this safer command.
- Added dashboard/status packet-recording metadata: briefing-feedback and public-feedback queue items now expose deterministic packet artifact paths plus packet-based record command templates, and the Web Review Queues verifier fails if those safer commands disappear.
- Updated the live remaining-gates handoff to prefer packet-based record commands after packet preparation for public feedback, customer discovery, and briefing feedback, while leaving explicit slug/item/source commands available as fallbacks.
- Updated weekly north-star report next actions to include packet-based public-feedback and briefing-feedback recording commands from the live review queues, and regenerated the live `north-star-week-2026-05-21.md` artifact.
- Tightened weekly north-star report external-feedback next actions so they remain visible until the V3.1 3-event standard is met, not only when feedback is zero; the report now states the remaining feedback-event count and falls back to customer-discovery packet/record commands when no public-feedback follow-up queue item is available.
- Added a first-class `customer_discovery_feedback` dashboard queue while the external-feedback count remains below 3; the queue now exposes the default `a2a_trust_manifest` question, deterministic packet artifact, packet preparation command, explicit record command, and packet-based record command. The Web Review Queues verifier now reports `customer_discovery_feedback:1`, `context_complete=11/11`, and `queue_specific_checks=13/13`, and both the weekly north-star report and remaining-gates handoff consume that queue metadata instead of relying only on static fallback commands.
- Updated weekly north-star next actions to show customer-discovery feedback as a parallel collection path even when a public-feedback follow-up queue item exists, so the 3-event external-feedback gate is not operationally bottlenecked on the published writeup receiving comments.
- Updated weekly north-star provider next actions to include the scoped non-mutating production canary dry-run command between scoped readiness and the mutating canary command, matching the safer provider-readiness sequence already documented in the remaining-gates handoff.
- Updated weekly north-star briefing-feedback next actions to show the queued blind-sample count, the first item packet-preparation command, and the bulk `v3_prepare_briefing_feedback_packet.py --all --json` command when multiple unreviewed briefing items are waiting.
- Updated human-readable `v3_status` output to include a non-empty review-queue breakdown, so operators can see active queues such as `briefing_feedback:5`, `customer_discovery_feedback:1`, `provider_provisioning:1`, and `public_feedback_followup:1` without opening the JSON dashboard.
- Updated human-readable `v3_status` output to point directly to the generated remaining-gates handoff and weekly north-star report directory, making the CLI a complete entry point for the current operator-gated closure workflow.
- Updated human-readable `v3_status` output to resolve and display the latest weekly north-star report file in addition to the report directory, while still falling back to the current-date artifact path if no report has been generated yet.
- Updated human-readable `v3_status` output to include compact north-star progress counters: public writeups, external feedback against the 3-event standard, and briefing-feedback count/coverage.
- Updated human-readable `v3_status` output to surface the selected provider first canary and scoped missing env vars, currently `tts` with `MIRA_TTS_ADAPTER_ENDPOINT` and `MIRA_TTS_ADAPTER_TOKEN`.
- Updated human-readable `v3_status` output to surface active external-feedback collection paths, currently the recorded public writeup slug `v31_green_dot_is_not_evidence` and customer-discovery topic `a2a_trust_manifest`.
- Updated human-readable `v3_status` output to surface queued publication-review drafts as external-feedback paths without counting them as shipped writeups, currently `publication_review=a2a_manifest_note`.
- Added `v3_prepare_north_star_closure_packets.py`, a no-network operator-prep command that writes one manifest plus publication-review, public-feedback, customer-discovery, and briefing-feedback packets for the current north-star gates without recording evidence or contacting external systems.
- Tightened the Web Review Queues implementation-status verifier so the remaining-gates handoff fails if the all-in-one north-star closure packet command disappears.
- Updated human-readable `v3_status` output to point directly to the latest generated north-star closure packet manifest and checklist.
- Updated north-star closure publication packets to expose the hash-bound `v3_record_public_evidence.py` command as first-class JSON output and in the generated closure checklist, so operators do not have to open packet metadata by hand after publication.
- Added packet-based public-writeup evidence recording across the dashboard, `v3_status --actions`, weekly report, remaining-gates handoff, and Web Review Queues verifier: public-writeup review items now expose the deterministic `publication_packet.json` path and prefer `v3_record_public_evidence.py --packet <publication_packet.json> --published-url <url> --feedback-source <source> --json`, while keeping the hash-bound explicit slug/draft command as a fallback.
- Updated public-feedback, customer-discovery, and briefing-feedback packet JSON plus the generated closure checklist to expose and prefer packet-based record commands, reducing manual slug/item-id copying during external-feedback and briefing-feedback gate closure.
- Updated human-readable `v3_status` output to surface the next briefing-feedback action: queued blind-sample count, first item id, and first packet artifact path.
- Added `v3_status --actions`, which appends first safe action commands from live review queues: public-writeup safety, publication-packet, and post-publication evidence commands; public-feedback packet preparation and packet-based recording; customer-discovery packet preparation and packet-based recording; first-item and bulk briefing-feedback packet preparation plus packet-based recording; full provider env-template/runbook generation; scoped provider env-template generation; scoped provider readiness; and scoped non-mutating provider canary dry-run. The action section explicitly warns operators to replace placeholder values such as `<url>`, `<source>`, `<insight>`, and `<button>` before running commands.
- Updated the generated remaining-gates handoff verification sequence to include `v3_status.py --actions` before JSON/status/report/test checks, and tightened the Web Review Queues verifier so the handoff fails if the actionable status entry point disappears.
- Updated the weekly north-star report next-action section to lead with the same live `v3_status.py --actions` closure command whenever public feedback, briefing feedback, provider readiness, or publication work is still pending, then regenerated the live `north-star-week-2026-05-21.md` artifact.
- Updated `v3_status --actions` to mirror the remaining-gates effect-reconciliation guidance for operator-supplied evidence files, so the status CLI now surfaces the `--publish-manifest`, `--rss-feed`, and `--provider-state-manifest` inspector options instead of requiring operators to open the handoff doc first.
- Updated the weekly north-star report next actions to mirror the same effect-reconciliation evidence-file guidance, keeping `v3_status --actions`, the remaining-gates handoff, and the weekly report aligned for unresolved side-effect review.
- Added a read-only effect-reconciliation inspection CLI, `v3_effect_reconciliation.py`, that renders the selected effect row, replay-bundle readability, replay-bundle provider-evidence status, optional Substack publication manifest and RSS feed matches, local or explicit provider-state JSON match status, and safe next steps without mutating the effect log. The dashboard `effect_reconciliation` queue and `v3_status --actions` now expose the inspection command, and the Web Review Queues verifier requires it.
- Extended the Web Review Queues implementation-status verifier to assert the remaining-gates handoff keeps the effect-reconciliation section, read-only inspector command, and warning against retrying or completing effects from local intent alone.
- Recorded existing V3.1 public-writeup evidence for `v31_green_dot_is_not_evidence` from `artifacts/writings/v31-green-dot-is-not-evidence/published.json` and the safety-audited `final.md`; the weekly report now asks for feedback on that recorded public writeup instead of borrowing the unpublished A2A draft's feedback slug.
- Live A2A strategic run: `a2a_trust_experiment_4eec26109610` wrote `a2a_trust_experiment.md`, `a2a_commercial_options.md`, and `a2a_product_thesis.md`, emitted `product_thesis:a2a_validator_api`, and added a second evidence-for item to `hypothesis:a2a_trust_manifest`.
- Live A2A public-writeup draft run: `a2a_trust_experiment_344a748f35cd` wrote `a2a_public_writeup_draft.md` with preview hash `1b9fcd57bb457f8188728cd19ef9b94d82e2b92fa7337018a5ef9e787aca3ef7`, emitted `public_writeup_plan:a2a_manifest_note` but no `public_writeup:*` completion ref, and added a third evidence-for item to `hypothesis:a2a_trust_manifest`.
- `git diff --check`: clean.
- Live dashboard evidence counts: L3 = 33, L4 = 27. `causal_link_validity` = 1.0 and `l4_required_causal_evidence` = 1.0.
- Live repeat-run smoke checks confirmed `system_health` and `intelligence_briefing` each persisted one L3 causal evidence record.
- Live article publish smoke created an `effect_reconciliation` queue item for staged `publish_substack` rather than recording a false successful publish.
- Live dashboard queues now separate approvals/effect reconciliation from incidents: approval queue empty, incident DLQ empty, and the current effect reconciliation queue contains one planned staged `publish_substack` item with recovered replay-bundle evidence rather than a false successful publish.
- Latest live `self_evolution` run produced both `self_evolution_experiment.md` and `self_evolution_canary.md`, and the experiment queue shows 2 evidence-for items plus reviewable current metric and rollback-plan fields for `hypothesis:self_evolution_pack_coverage`.
- Latest live `a2a_trust_experiment` audit artifact verifies successfully with `verify_workflow_audit_artifact`, and the latest run produced `a2a_commercial_options.md` with two explicit commercial-option refs plus a durable evidence-for entry on `hypothesis:a2a_trust_manifest`.

## Remaining Gaps

1. Most newly covered workflows are safe local MVPs. Podcast TTS routing/synthesis staging/local command execution/hosted HTTPS execution, podcast RSS staging/local feed publication/hosted HTTPS publication, social publish staging/hosted HTTPS execution, market alert staging/provider-state/hosted HTTPS execution, health external-write staging/provider-state/hosted HTTPS execution, destructive memory compaction, and self-evolution production promotion are now memory-driven or connector-aware where applicable, approval-gated, and auditable. Generic injected/config-backed HTTPS provider execution exists for approved RSS/social/market/health/Substack/TTS/deployment/deployment-health/deployment-rollback effects, approved preview payloads are sent to configured write adapters, local provider-state execution exists for non-RSS effects, local command and hosted HTTP TTS execution exist, local and hosted HTTP RSS publication are concrete, hosted HTTP social/market/health writes are concrete, git remote branch promotion/rollback is concrete, deployment-service orchestration plus health verification plus non-git rollback orchestration are concrete, local deployment command execution is concrete, local provider dress rehearsal is executable, selected-provider production canary orchestration is executable once readiness passes, and provider readiness has an explicit audit gate, bootstrapped no-secret configs, and a generated no-secret provisioning template. The project still needs real production endpoint/credential/script provisioning outside the no-secret placeholders; the current readiness CLI proves endpoint/token env vars remain unset in this workspace.
2. L4 coverage now spans native `communication` and all executable workflow-pack repeated runs when prior same-pipeline context changes deterministic behavior. Structural causal traces, Eval 1 failure-signature recurrence/prevention metrics, artifact-backed voice stability, briefing interest fit, briefing feedback-button metrics, weekly blind-sample review selection, Eval 7 memory audit metrics, and causal-memory changed-strategy scoring now pass in the bounded weekly report. The report now separates failure-memory capture and first-run baseline artifacts from actual missing causal-link gaps; current actionable causal coverage gaps are zero, while raw causal-link coverage remains intentionally low because routine/baseline records are not forced to claim causal memory influence.
3. Eval 6 now has explicit V3.1 `IncidentEvent` coverage, and the bounded weekly report passes after expected human gates, preflight guardrails, and known test task records are separated from live incidents. The project should keep monitoring test-record leakage and true incident recurrence, but the remaining approval-burden blocker is no longer a local Eval 6 scorecard failure.
4. Security checks are still deterministic and pattern-based. Policy-bypass intent, positive/negative semantic contradictions, public/private routing reversals, and conflicting numeric operating constraints are now review-gated, but this is not a general natural-language theorem prover.
5. Audit artifact key rotation, trust-bundle provenance, trusted-operator import policy, and explicit symmetric activation controls are implemented for HMAC signatures. This is still symmetric local trust; public-key verification is not implemented because the project currently has no asymmetric signing dependency in the local environment.
6. Self-evolution now has a local canary/rollback phase, N-run observation, opt-in local git branch canary execution, approval-gated automatic promotion staging, approval-gated local production promotion/rollback adapter execution, explicit remote git branch promotion/rollback orchestration, opt-in deployment-service execution after promotion, opt-in deployment health verification, and opt-in non-git deployment rollback orchestration.
7. Effect reconciliation now carries replay bundle refs and provider refs, duplicate retries are blocked, local Substack/RSS/social/market/health state can reconcile open effects, connector-backed resolver hooks exist, generic read-only HTTP JSON provider resolvers and write-side HTTPS JSON provider adapters have env-backed production profiles and production-safety validation, approved preview artifacts are included in adapter request bodies, local provider-state execution is implemented and dress-rehearsed, local command and hosted HTTP TTS execution are implemented, local plus hosted HTTP RSS publication is implemented, hosted HTTP social/market/health writes are implemented, local deployment command adapters are implemented, and production readiness is machine-checkable. Legacy replay gaps in the current log were recovered from durable checkpoints/provider-state evidence with append-only effect-log annotations, and local replay-bundle refs are now validated for readable JSON, required replay fields, compensation strategy, and idempotency-key binding. Credentialed live endpoints and local production scripts still need environment-specific provisioning.

## Next Move

Move from local dress rehearsal to credentialed production dress rehearsal while clearing the human-feedback gates tracked in `docs/v31-north-star-remaining-gates-2026-05-21.md`. Provision the smallest current canary scope first, TTS, by filling `data/v3/provider_provisioning.tts.template` in the real shell/launchd/secret-manager environment for `MIRA_TTS_ADAPTER_ENDPOINT` and `MIRA_TTS_ADAPTER_TOKEN`. Rerun `v3_provider_readiness --skip-resolvers --require-adapter tts --json` until it returns `ready: true`, then run `v3_provider_production_canary --provider tts --json` against the real provider through the effect log and reconciliation queues. Keep every promotion behind approval, audit artifacts, effect logs, and causal evidence.
