[pytest]
markers =
    slow: marks tests that use real LLM calls (costs tokens)
    pipeline: end-to-end publish pipeline smoke tests
testpaths = agents
addopts = --import-mode=importlib
python_files = test_*.py
python_classes = Test*
python_functions = test_*
